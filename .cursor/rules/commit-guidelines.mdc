---
name: commit-guidelines
description: PickOne commit and verification compatibility entrypoint
alwaysApply: false
---

# PickOne Commits

Read the commit requirements in `docs/process/agent-delivery-model.md`, the
verification contract in `docs/process/repository-verification.md`, and the
identity policy in `docs/process/github-app-authentication.md` before committing.

Do not duplicate commit policy here.
